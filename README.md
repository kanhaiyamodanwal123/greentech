# Bike/EV Rental Platform (MERN + EJS)

## Setup

1. Install Node.js (v16+ recommended) and MongoDB.
2. Clone / copy project files.
3. Create `.env` from `.env.example` and set values.
4. Install dependencies:
